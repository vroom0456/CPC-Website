// ===========================================
// FRONTEND CONFIGURATION
// The Google Drive API key and event list now live in the backend/db —
// the frontend only needs to know where the API is.
// ===========================================
const CONFIG = {
  API_BASE_URL: 'http://localhost:4000/api'
};
